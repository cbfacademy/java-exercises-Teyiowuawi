package com.cbfacademy.shapes;

import java.util.ArrayList;
import java.util.List;

public class Shapes {

    public void computeArea(List<Enclosure> enclosure){
       //ArrayList<Double> shapesArea = new ArrayList<>();
        //take a list of object that implement enclosure - as a parameter
        // use area method on this group of objects then add the area for each shape together   
        for (int i = 0; i < enclosure.size(); i++) {
           //
        }

        }
        

    public void computePerimeter(List<Enclosure> enclosure){
        for (int i = 0; i < enclosure.size(); i++){
//
        }
        // //take a list of object that implement enclosure 
        // use perimeter method on this group of objects     
    }


    public static void main(String[] args) {
        System.out.println();
    }
}
